defaults:
  - _self_
  - data: dataset
  - store: features
  - models: lightgbm
  - override hydra/hydra_logging: disabled
  - override hydra/job_logging: disabled

hydra:
  run:
    dir: .
  output_subdir: null

output:
  path: output
  submission: sample_submission
  name: 5fold-ctr-lightgbm

seed: 42